<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN" "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">
<html xmlns="http://www.w3.org/1999/xhtml" >
  <head>
    <meta name="keywords" content="" />
    <meta name="description" content="" />
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8" />
    <title>Art Around</title>
    
    <%= stylesheet_link_tag 'main' %>
    
    <%= javascript_include_tag 'jquery-1.4.2.min' %>
    <%= javascript_include_tag 'flickr' %>
    <%= javascript_include_tag 'artaround' %>
    
    <script type="text/javascript">
      Flickr.api_key = "<%= flickr[:account][:api_key] %>";
      
      $(function() {
        // load thumbnails into recent art frames
        <% recent_art.each do |art| %>
          <% if art.flickr_ids and art.flickr_ids.any? %>
            loadThumbnail("art-thumbnail-<%= art.id %>", "<%= art.flickr_ids.first %>");
          <% end %>
        <% end %>
      });
    </script>
    
    <%= yield :head %>

  </head>
  
  <body class="<%= content_for :body_class %>">
  
    <div id="headerWrapper">
      <div id="pageMain">
        <div id="header">
          <h1><a href="/">Art Around</a></h1>
          <div id="nav">
            <ul>
              <li id="about"><a href="#">About</a></li>
              <li><a href="<%= contact_path %>">Contact Us</a></li>
            </ul>
            <div class="clear"></div> 
          </div>
          
          <% [:notice, :alert].each do |msg| %>
            <% if flash[msg] %>
              <div class="flash <%= msg %>">
                <%= flash[msg] %>
              </div>
            <% end %>
          <% end %>
          
          <!-- used on home page for featured box -->
          <%= yield :header %>
        </div>
        
        <%= yield %>
        
        <div class="clear"></div>
      </div>
    </div>
  
    <!-- used on home page for map container -->
    <%= yield :bottom %>
    
    <div id="footerWrapper">
      <div id="footer">
        <h3>Recent Uploads</h3>
        <ul>
          <% recent_art.each do |art| %>
            <li>
              <a href="<%= art_path(:id => art.id) %>" id="art-thumbnail-<%= art.id %>">
                <%= image_tag 'placeholder.jpg' %>
              </a>
            </li>
          <% end %>
        </ul>
      </div>
    </div>
    
    <%= yield :footer %>
    
  </body>
</html>