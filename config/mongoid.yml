defaults: &defaults
  host: localhost
  raise_not_found_error: false


development:
  <<: *defaults
  database: artaround

# set these environment variables on your prod server
production:
  host: <%= ENV['ARTAROUND_MONGOID_HOST'] %>
  port: <%= ENV['ARTAROUND_MONGOID_PORT'] %>
  database: <%= ENV['ARTAROUND_MONGOID_DATABASE'] %>

